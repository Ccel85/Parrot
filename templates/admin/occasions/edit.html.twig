{% extends 'base.html.twig' %}
{% block title %}Nos Occasions{% endblock %}

{% block body %}
<div class="w-50 m-auto">
  {{ form_start(form) }}
  {{ form_widget(form) }}
    <button type = "submit" class="btn btn-warning " name="action" value="save" >Modifier</button>
    <button type = "submit" class="btn btn-warning " name="action" value="delete" >Supprimer</button>
    <a href="{{ path('app_occasions_show') }}" class="button">Retour</a>
  {{ form_end(form) }}
</div>
{% endblock %}