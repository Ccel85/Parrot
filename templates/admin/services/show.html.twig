{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block body %}
  <div>
    <h1 class= "text-center">Services</h1>
    {% if services is empty %}
      <p>Pas de données à afficher</p>
    {% else %}
      <div class="container">
        <div class="row">
          {% for service in services %}
            <div class="col-md-6 mb-3">
              <div class="card ">
                <img src="{{ asset( service.pathimage) }}" style="max-width: 400px; max-height: 400px;" class="card-img-top m-auto" alt="...">
                <div class="card-body ">
                  <small class="text-muted ">Référence: {{ service.id }}</small>
                  <h1 class="card-title ">Titre: {{ service.title }}</h1>
                  <p class="card-text">Description:<br> {{ service.description }}</p>
                  <div class="card-footer mt-auto">
                    <div class="form-check form-switch d-flex align-items-center ">
                      <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault{{ service.id }}" {% if service.isPublished %}checked{% endif %} disabled />
                      <label class="form-check-label ms-2" for="flexSwitchCheckDefault{{ service.id }}">Publier</label>
                    </div>
                    <a href="{{ path('app_admin_services_edit', {'id': service.id}) }}" class="btn btn-primary">Modifier</a>
                  </div>
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    {% endif %}
    <a href="{{ path('app_admin_services_new') }}" class="button">Nouveau service</a>
  </div>
{% endblock %}


